<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tarena.fh.mapper.OndutyMapper">

	<!-- 查询所有值班记录 -->
	<select id="findAll" resultType="Onduty">
		SELECT * FROM onduty
	</select>

	<!--  新增值班记录 -->
	<insert id="saveOnduty">
		INSERT INTO onduty(ondutyTime,ondutyNum,ondutyLeader,remark)
		VALUES (
			#{ ondutyTime },
			#{ ondutyNum },
			#{ ondutyLeader },
			#{ remark }
		)
	</insert>

	<!--  根据OndutyId查询Onduty -->
	<select id="findOndutyByOndutyId" resultType="Onduty">
		SELECT * FROM onduty WHERE ondutyId=#{ondutyId}
	</select>

	<!-- 更新Onduty -->
	<update id="updateOnduty">
		UPDATE onduty SET
    		ondutyLeader=#{ ondutyLeader },
     		ondutyTime=#{ ondutyTime },
      		ondutyNum=#{ ondutyNum },
			remark=#{ remark }
		WHERE ondutyId=#{ ondutyId }
	</update>

	<!-- 批量删除 -->
	<delete id="deleteOnduty">
		DELETE FROM onduty WHERE ondutyId in(
			<foreach collection="array" item="ondutyId" separator=",">
				#{ ondutyId }
			</foreach>
		)
	</delete>
</mapper>